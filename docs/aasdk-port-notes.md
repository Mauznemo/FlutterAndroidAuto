# Porting aasdk to Boost 1.87 or newer, and CMake 4

What actually broke when building `opencardev/aasdk` (pinned at `9bf6adf`) against a
current toolchain, and what the fix was. The constraints are the two in the title:
Boost 1.87 removed `io_service`, and CMake 4 dropped compatibility with the very old
`cmake_minimum_required` the project asks for. Neither is distribution specific, and
most distributions have crossed both by now.

Verified against Boost 1.90, protobuf 3.21.12, OpenSSL 3.5.5, libusb 1.0.29, CMake 4.2.3
and GCC 15.2, on Ubuntu 26.04.

The whole port lives in
`packages/android_auto_linux/linux/patches/0001-port-to-boost-asio-io_context.patch`,
regenerated by `tools/port-aasdk.sh`. The submodule is never committed dirty.

## Result

```
$ tools/build-aasdk.sh
boost      : 1.90.0
channel    : MEDIA_SINK_VIDEO
strand     : dispatch, post and get_io_service all behave
protobuf   : ServiceDiscoveryRequest round trips
OK
```

System protobuf works, so `-DSKIP_BUILD_PROTOBUF=ON -DSKIP_BUILD_ABSL=ON` avoids aasdk
building its own protobuf 30. 254 generated `.pb.h` files, covering every channel this
plugin speaks. Stripped output is 1.6 MB of `libaasdk.so` plus 2.1 MB of
`libaap_protobuf.so`, which is small enough that shipping them beside the plugin is fine
and static linking is not worth patching upstream for.

## The five failures, in the order they appeared

### 1. CMake 4 dropped `cmake_minimum_required(VERSION < 3.5)`

```
CMake Error at cmake_modules/gitversion.cmake:2 (cmake_minimum_required):
  Compatibility with CMake < 3.5 has been removed from CMake.
```

Three files still asked for 3.0.0 or 3.10. Raised to 3.16, which is what the top level
already required.

### 2. Boost 1.90 no longer ships a `system` component

```
Could not find a package configuration file provided by "boost_system"
```

Boost.System has been header only since 1.69 and the compiled library and its CMake
component are finally gone. `find_package(Boost REQUIRED COMPONENTS system log_setup log)`
drops `system`; `Boost::headers` already covers what aasdk used it for.

### 3. `boost::asio::io_service` is gone

Deprecated in 1.66, removed in 1.87. 95 files, 64 uses of the type itself and 67 of
`io_service::strand`.

The obvious port, rewriting every construction, dispatch and accessor call site, is a
large and risky diff. Instead the patch adds `aasdk::Strand`, a thin subclass of
`boost::asio::strand<io_context::executor_type>` that restores the three pieces of the
old API aasdk actually uses:

| Old `io_service::strand` | New `strand<Executor>` | What `aasdk::Strand` does |
|---|---|---|
| `strand(io_service&)` | built from an executor | constructor taking `io_context&` |
| `dispatch(h)`, `post(h)` | both require an allocator | one argument overloads via the free functions |
| `get_io_service()` | gone | restored |
| `context()` returns `io_context&` | returns `execution_context&` | narrowed with a downcast |

After that the port is three textual substitutions:

```
boost::asio::io_service::strand        ->  aasdk::Strand
boost::asio::io_service                ->  boost::asio::io_context
boost::asio::ip::address::from_string  ->  boost::asio::ip::make_address
```

plus an `#include <aasdk/Common/Strand.hpp>` in the files that name the type.

The substitution order matters. `io_service::strand` has to be replaced first, otherwise
it becomes `io_context::strand`, which does not exist.

### 4. `io_context` has no `post()` or `dispatch()` members either

```
error: 'class boost::asio::io_context' has no member named 'dispatch'
```

`IOContextWrapper` calls them on the `io_context` directly, where there is no subclass to
hide behind. Those two call sites moved to `boost::asio::post(ctx, h)` and
`boost::asio::dispatch(ctx, h)`.

### 5. `strand.context()` is typed `execution_context&`

```
error: no matching function for call to
  'aasdk::io::Promise<void>::defer(boost::asio::execution_context&)'
```

aasdk already guards this one on `BOOST_VERSION`: below 1.66 it calls
`strand_.get_io_service()`, above it `strand_.context()`. The newer branch compiles but
returns the wrong type, because `strand<Executor>::context()` is typed
`execution_context&` and every aasdk consumer wants `io_context&`. `aasdk::Strand`
narrows both accessors with a downcast, which is sound because every `Strand` is
constructed from an `io_context`.

## One thing that is not a Boost problem

`aasdk` exposes its headers with a directory scoped `include_directories()`, which does
not reach a parent project's targets. Anything consuming aasdk through
`add_subdirectory()`, which is exactly how the Flutter plugin consumes it, cannot find
`aasdk/...` headers at all. The patch adds the include directory to the `aasdk` target
itself, where it belongs.

## Worth sending upstream

Items 1, 2 and the include directory fix are plain portability bugs that would help
`opencardev/aasdk` on any modern distro. Items 3 to 5 are the real port and are more
opinionated, since upstream may prefer rewriting the call sites over the `aasdk::Strand`
shim. Offer 1, 2 and the includes as a small PR first.
