# Dart / Flutter
.dart_tool/
.packages
.pub-cache/
.pub/
build/
pubspec.lock
# ...except the example, which is an application rather than a library, and so commits
# its lockfile to keep a build reproducible.
!example/pubspec.lock
*.iml
.idea/
.vscode/
.flutter-plugins
.flutter-plugins-dependencies

# Generated plugin registrants, regenerated by the tooling on every build
example/linux/flutter/generated_plugin_registrant.cc
example/linux/flutter/generated_plugin_registrant.h
example/linux/flutter/generated_plugins.cmake

# Native build output
packages/android_auto_linux/linux/third_party/*/build/
compile_commands.json

# Custom head unit certificates, never commit these
**/custom_headunit.crt
**/custom_headunit.key

# OS
.DS_Store
